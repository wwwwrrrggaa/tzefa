# SOTA OCR Pipeline Requirements
# State-of-the-art models for document processing and Tzefa OCR

# === CORE DEEP LEARNING ===
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0

# === SOTA MODEL DEPENDENCIES ===
# T2T-BinFormer (SOTA Document Binarization)
timm>=0.9.0                    # Transformer models
einops>=0.6.0                  # Tensor operations

# DocDiff (Diffusion Document Enhancement)
diffusers>=0.20.0              # Diffusion models
accelerate>=0.20.0             # Model acceleration

# DocEnTR (Vision Transformer Enhancement)
vit-pytorch>=0.35.0            # Vision transformers

# === FALLBACK MODELS ===
# Advanced Document Preprocessing
doctr[torch]>=0.7.0
easyocr>=1.7.0
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# Document Layout Analysis
layoutlm>=3.0.0
layoutlmv2>=0.1.0
layoutlmv3>=0.1.0

# === COMPUTER VISION ===
opencv-python>=4.8.0
pillow>=9.5.0
numpy>=1.24.0
scikit-image>=0.21.0
albumentations>=1.3.0

# === UTILITIES ===
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
requests>=2.31.0
pathlib

# === OPTIONAL GPU ACCELERATION ===
# Uncomment if using CUDA
# torch-audio                  # Audio processing (if needed)
# tensorrt                     # NVIDIA TensorRT
# onnxruntime-gpu             # ONNX GPU runtime
